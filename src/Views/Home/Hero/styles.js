import styled from "@emotion/styled";
import HeroImage from '../../../assets/pics/gradient.jpg'

export const Hero = styled.div({
    width: '100%',
    height: '100vh',
    backgroundImage: `url(${HeroImage})`,
    backgroundSize: 'cover',
})
